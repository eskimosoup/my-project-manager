<div class="content">
  <div class="index">
    <h1>
      <%= link_to "Edit", edit_customer_path(@customer), class: "action-button" %>
      <%= @customer.name %>
    </h1>

    <%= render "customers/projects", customer: @customer %>
    <%= render "customers/addresses", customer: @customer %>
  </div>
</div>
