<%= simple_form_for @invoice_search do |f| %>
  <%= f.input :paid, collection: InvoiceSearch::STATUSES %>
  <%= f.input :customer_id, collection: Customer.order(:name).pluck(:name, :id) %>
  <%= f.button :submit %>
<% end %>
