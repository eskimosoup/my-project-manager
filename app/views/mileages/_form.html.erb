<%= simple_form_for [@print_job, @mileage] do |f| %>
  <%= f.input :miles, disabled: disabled_form_field(@mileage.print_job) %>
  <%= f.input :actual_miles if show_actualised_fields(@mileage.print_job) %>
  <%= f.button :submit %>
<% end %>
