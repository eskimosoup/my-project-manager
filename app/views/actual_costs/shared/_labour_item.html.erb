<%= simple_form_for labour_item, remote: true do |f| %>
  <%= f.input :hours, disabled: true %>
  <%= f.association :labour, disabled: true %>
  <%= f.input :actual_hours %>
  <%= f.input :actual_cost, label: 'Actual cost (automatically calculated if left empty)' %>
  <%= f.button :submit %>
<% end %>
